specVersion: "v1beta"
serviceId: "Face authenticator"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Face authenticator-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
